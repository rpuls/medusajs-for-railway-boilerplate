export default formatResourceName;
/**
 * Formats a filename into the correct resource name.
 * Names are camelCase formatted and namespaced by the folder i.e:
 * admin/product-variants -> adminProductVariants
 * @param {string} fn - the full path of the resource
 * @return {string} the formatted name
 */
declare function formatResourceName(fn: string): string;
